object Q1_2_3 {
    def main(args: Array[String]) = {
        var k, i, j = 2;
        var m, n = 5;
        var f = 12.0f;
        var g = 4.0f;
        var c = 'X';

        println("a) k + 12 * m = " + (k + 12 * m) ); // 62
        println("b) m / j = " + (m / j) ); // 2
        println("c) n % j = " + (n % j) ); // 1
        println("d) m / j * j = " + (m / j * j) ); // 4
        println("e) f + 10*5 +g = " + (f + 10*5 +g) ); // 66.0
        i += 1;
        println("f) ++i * n = " + (i * n) ); //15
    }
}